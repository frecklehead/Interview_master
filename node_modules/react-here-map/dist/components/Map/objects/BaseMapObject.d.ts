/// <reference types="heremaps" />
import { IHMapState } from '../Map';
import { mapEvents } from '../../libs/defaults';
declare type Object = H.map.Circle | H.map.Marker | H.map.Polyline | H.map.Polygon;
interface Props {
    initializeFn: (ctx: IHMapState) => Object;
    initializeDeps: any;
    events: typeof mapEvents;
    zoom?: number;
}
export declare const BaseMapObject: ({ initializeFn, initializeDeps, events, zoom, }: Props) => null;
export {};
